spring:
  datasource:
    url: jdbc:mysql://localhost:3306/malllearning?useSSL=false&serverTimezone=Asia/Shanghai
    username: root           # ??????
    password: root           # ?????
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10  # ????????

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true           # ????? SQL ??
    properties:
      hibernate:
        format_sql: true      # ??? SQL ??
# application.yml
# Spring Boot 3.x + OpenAPI3 配置
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  packages-to-scan: com.example.malllearning.controller
