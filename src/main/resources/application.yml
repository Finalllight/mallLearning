spring:
  datasource:
    url: jdbc:mysql://localhost:3306/malllearning?useSSL=false&serverTimezone=Asia/Shanghai
    username: root           # ??????
    password: root           # ?????
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10  # ????????

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true           # ????? SQL ??
    properties:
      hibernate:
        format_sql: true      # ??? SQL ??
# application.yml
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs

knife4j:
  enable: true
  setting:
    language: zh_cn   # 中文界面
